"""Legacy presentation modules kept for reference.

Do not import from here in new code. These modules are preserved only so
past work (standalone Gradio dashboard with its own WebRTC/HTTP streaming
loop) is not lost. The real dashboard lives in ``run_dashboard.py`` at the
repo root and uses the native PreviewSink for video output.
"""
